<UserControl x:Class="Pete.Views.TaskScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:res="clr-namespace:Pete.Resources"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="1" Text="{Binding Status}" Margin="0,0,0,20" Foreground="{DynamicResource Brush.Text}" FontWeight="Medium" FontSize="32" TextAlignment="Center"/>
        <res:Spinner Grid.Row="2"/>
    </Grid>
</UserControl>
